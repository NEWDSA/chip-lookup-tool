# ChipLookup UI 全面评估报告

> 评估日期：2026-09-09 · 评估对象：`src/ui.py`（Tkinter，深色主题，约 2200 行）
> 评估方法：代码走查 + 三种状态实机截图（`ui_review_default_full.png` 默认 1040x720 / `ui_review_minimal.png` 极简 / `ui_review_minsize.png` 最小 1000x680）+ WCAG 2.1 对比度实测（12 组配色）
> 优先级定义：**P0** = 影响核心任务完成度或全局质感，建议立即处理；**P1** = 高频使用痛点或可达性硬伤；**P2** = 打磨项，择机处理
>
> **状态更新（2026-09-09）：全部 20 项已处理完毕**——P0 三项、P1 八项（#1/#2/#3/#4/#6/#7/#8/#11/#12）、第三批（#5/#9/#10）与 P2 打磨项（#13~#20）均已修复并通过回归；唯一例外：#18 的浅色主题按原评审定位为「长期项」，本轮落地了版本号与「关于」入口。详见各条 ✅ 标注。

---

## 总体评价

底子不错：深色主题有完整的 Design Token（字号/间距/按钮三规格）、12 组关键配色实测 11 组达 WCAG AA、键盘快捷键体系完整（↑↓/Enter/Esc/F5/Ctrl+C）、自绘表头做到像素级对齐、数据源切换已后台线程化、极简模式是亮点。**主要短板集中在：默认布局下核心数据被裁切、DPI 感知缺失导致的整体模糊、键盘焦点不可见、目标尺寸过小、信息架构未分主次。**

| 维度 | 评级 | 一句话结论 |
|------|------|-----------|
| 视觉风格 | B+ | Token 化好、对比度达标；输在 DPI 模糊与图标缺失 |
| 布局结构 | B- | 三段式清晰；输在底栏平铺、顶栏拥挤、列宽策略 |
| 交互体验 | B+ | 快捷键全、反馈基本齐；输在忙碌指示与极简态反馈 |
| 响应式适配 | C+ | 分栏/极简好；输在 minsize 过大、DPI、列裁切 |
| 可访问性 | C+ | 对比度达标；输在焦点不可见、目标尺寸、无 tooltip |

---

## P0 —— 立即处理

### 1. 完整模式下候选列表数据严重裁切，核心任务不可完成 ✅ 已修复
- **现象**（原 `ui_review_default_full.png`）：默认 1040 宽下左栏约 316px，料号列仅 ~24px 只显示 2 个字符（"K₂"）；型号/厂商/类型同样被裁。查询工具的列表里**看不到完整料号**，等于核心功能残废。
- **根因**（修正）：`_fit_tree_columns` 在「可用宽 < Σ最小宽」的缺口场景下，把缺口全部记在料号列头上（`plan["part_number"] = avail - sum(其余最小宽)`，其中容量列独占 130px），料号列被吸干至 24px。
- **修复**：①列宽策略改三档（PREFERRED 常规最小 / FLOOR 硬底线 / DEFICIT_W 缺口权重），缺口时从硬底线起步、富余按权重分配（料号 0.46 最高、单列封顶），极窄时按底线等比压缩；②左栏默认占比 30%→40%（1040 宽下 avail≈364px，料号完整宽 128 + 其余列底线可同屏）；③自定义列宽分支改 FLOOR 托底，冲突时回落标准分配；④全部宽度常量随 DPI 缩放。
- **验证**：新增 `tests/test_tree_columns.py` 5 用例全绿（常规/缺口/极窄/DPI 缩放/自定义冲突）；修复后默认视图料号列显示 14 字符（"K4A4G165WF-BCW"），极简模式全 15 字符完整可读。

### 2. 全程无 DPI 感知，高分屏下整体发虚 ✅ 已修复
- **现象**：`main.py`/`ui.py` 均未调用 `SetProcessDpiAwareness`。用户屏幕为 125% 缩放，Tk 默认按 96DPI 渲染后被系统位图拉伸，全部文字与控件边缘模糊。
- **修复**：①`main.py` 入口 `_enable_dpi_awareness()`（创建 Tk 前调用 `SetProcessDpiAwareness(1)`，Vista 回退 `SetProcessDPIAware`，失败静默）；②`ui.py` 新增 `_ui_scale` 因子（`_win_dpi_scale()` 走 `GetDpiForSystem`/`GetDeviceCaps` 取真实 DPI——**不能**用 `winfo_fpixels('1i')`，它由字体度量推导实测 96.09/96，~1% 漂移会让全部像素常量偏移）；③窗口默认/最小/记忆尺寸改「逻辑像素」语义（配置跨缩放比例稳定），行高/表头高/列宽/分栏常量/滚动条全部经 `_s()` 缩放。
- **验证**：DPI 未感知进程 API 返回虚拟化 96 → 因子恒 1.0，测试环境行为与旧版完全一致；新增 3 个 125% 模拟用例（窗口缩放/逻辑像素存取/恢复）全绿。

### 3. 蓝色按钮白字对比度 3.30:1，不达 WCAG AA ✅ 已修复
- **现象**（实测）：`#ffffff` on `#4a90e0` = **3.30:1**（「搜索」「复制料号」按钮）。按钮文字 10pt 粗体不构成 WCAG"大字"豁免（需 ≥14pt 粗体）。其余 11 组配色全部达标（正文 12.8~13.9:1，次文字 6.8~8.1:1）。
- **修复**：白字改深色 `#0e1620`（对 #4a90e0 = 5.6:1 ✓，与 Accent 绿钮「亮底 + 深字」设计语言统一）；active 态底色由 `#2563eb`（深字仅 3.6:1 会倒挂）改浅蓝 `#6aa9ec`（深字 7.5:1 ✓）；disabled 态补 `#b0c4d4` 前景映射（底色偏深，深字仅 2:1）。
- **验证**：三态对比度逐态核对全部 ≥ 4.5:1；截图确认搜索/复制料号按钮深字渲染正常。

---

## P1 —— 高频痛点与可达性硬伤

### 4. 键盘焦点完全不可见 ✅ 已修复
- **现象**：输入框 `focuscolor=""` 显式去掉焦点框（ui.py:569），所有按钮 `focusthickness=0`。Tab 在控件间移动时**没有任何视觉指示**。
- **修复**：五个按钮样式（Accent/Blue/Ghost/ModeSel/ModeSelActive）`focusthickness 0→1` + `focuscolor=COLOR_FOCUS`（亮蓝 1px 焦点环，clam 主题 Button.focus 元素支持）；Search.TEntry 补 focus 态 `bordercolor`/`lightcolor` → COLOR_FOCUS 映射（与按钮焦点环同色系）。radio 保持无焦点框（其 layout 已移除 focus 元素，选中态高亮即位置指示）。
- **验证**：`ttk.Style().lookup()` 确认 focusthickness=1、focuscolor 生效；Tab 遍历按钮/输入框可见亮蓝描边。

### 5. minsize 1000x680 过大，主流分屏场景放不下 ✅ 已修复
- **现象**：1080p 显示器 125% 缩放下，半屏逻辑宽度仅 768px，当前 minsize 1000 根本无法贴靠半屏；与 Excel/文档对照使用（查询工具最高频场景之一）受限。
- **修复**：①minsize 降至 720x560（逻辑像素，随 DPI 缩放），1080p@125% 半屏可贴靠；②响应式收缩：状态栏文案改三态（≥1150 完整 / <1150 只留条数 / <900 清空文字给 radio 组让位，跨阈值实时联动）；③底栏低频项收进「更多 ⋯」菜单（见 #9），窄窗下底栏仅 5 个控件不溢出。
- **验证**：`test_size_clamped_to_minsize` 更新为 720x560 全绿；720x560 截图目检——顶栏/radio/底栏完整、列表按硬底线等比压缩仍可辨（配合行级悬停可看全料号）。

### 6. 点击目标过小：滚动条、分栏拖柄 ✅ 已修复
- **现象**：SlimVScrollbar 命中区 11px（视觉 6px）、分栏 sash 6px 宽；WCAG 2.5.8 建议目标 ≥24px。
- **修复**：①SlimVScrollbar `WIDTH 11→16`（视觉条 6px 居中绘制不变，纤细观感保留、整条命中区更易点）；②sash 由 6px Frame 改 **12px Canvas**（中间 6px 视觉条居中满高绘制，hover/拖动高亮只 `itemconfigure` 视觉条颜色，命中区恒定）；③顺带修复 sash 拖动范围 240/260/520 逻辑像素硬编码未随 DPI 缩放的问题。
- **验证**：探针实测 sash 命中区 12px、视觉条 6px 居中满高（coords [3,0,9,674]）；极简动画终点由「内宽-16」改为按实际分隔条宽计算（7+12+3）。

### 7. 无任何 tooltip，快捷键体系不可发现 ✅ 已修复
- **现象**：全应用无 tooltip（grep 零命中）。F5 刷新、Ctrl+C 复制料号、Esc 清空/退极简、双击看详情等能力只能靠文档或猜。
- **修复**：新增轻量 `ToolTip` 类（无边框深色 Toplevel + 600ms 防抖 + 指针右下弹出 + 屏幕边缘自动收敛 + `update_text` 动态文案）；挂接：顶栏极简按钮、3 个数据源 radio（各模式差异说明）、查询输入框、解析料号、搜索、分页 «/»、加载更多、底栏 7 钮（快捷键写入文案：复制料号 Ctrl+C、清空 Esc、刷新数据 F5）。候选列表另做**行级 Motion 提示**（停留 600ms 显示完整料号/厂商·容量，列窄被裁内容可悬停看全；拖列宽期间抑制）。
- **验证**：探针实测 ToolTip 防抖弹出/销毁、行级提示文本正确。

### 8. 顶栏状态文案冗长重复，窄窗时被挤压 ✅ 已修复
- **现象**（截图）：「模式 本地CSV · **本地CSV** · chip_database.csv · 共 32 条记录」——模式名与数据源描述重复出现"本地CSV"两次；右侧还有数据源三选钮 + 极简按钮，窄窗时状态文字最先被挤压截断。
- **修复**：常驻文案精简为「本地CSV · 共 N 条」（去重复的模式前缀与文件名）；数据源完整描述（LABEL · 文件名）移入状态栏悬停 tooltip（动态 `update_text` 同步）；窗口 < 逻辑 1150px 只留「共 N 条」，跨阈值往返由 `_on_window_configure` 联动重刷。切换完成 toast 同步去重（文件名悬停可见，toast 只说「已切换至「X」 · 1.2s」）。
- **验证**：探针实测 1200px↔1024px 往返文案正确切换；tooltip 文本与 describe() 一致。

### 9. 底部操作栏 7 个按钮平铺，主次不分、退出混排 ✅ 已修复
- **现象**（截图）：复制全部/复制料号/清空/导入CSV/导出CSV/刷新数据/退出等宽平铺；「退出」位于最右但与功能按钮同行同权重；低频的导入/导出/刷新占据大量视觉权重。
- **修复**：保留「复制全部/复制料号/清空」为实体按钮；导入/导出/刷新收进「更多 ⋯」深色 Menubutton（含快捷键标注与 tooltip）；「更多」菜单同时容纳界面缩放（#16）与关于（#18）；「退出」独占最右。底栏从 9 个控件减为 5 个。
- **验证**：探针确认底栏按钮集合正确、导入不再是实体按钮、菜单末项为「关于 ChipLookup」；720x560 窄窗底栏不溢出。

### 10. 「分页：[分页][加载更多]」暴露实现概念 ✅ 已修复
- **现象**（截图）：左栏常驻一行分段按钮让用户选"分页策略"，这是实现术语不是用户目标。
- **修复**：策略选择行整体移除，固定「加载更多」模式；追加改为自动——悬停到已加载末行、或键盘（↑↓）浏览到末行时自动加载下一页（焦点不回跳，浏览连续）；pager 行内的「加载更多（还剩 N 条）」按钮保留兜底（内容不满一屏无法触发自动加载时用）。分页渲染代码路径保留，将来可配置化再启用。
- **验证**：探针模拟「末行悬停」触发 `_maybe_autoload_more`，`_visible_count` 8→16 正确递增；列表区多出一行纵向空间。

### 11. 极简模式下操作无可见反馈 ✅ 已修复
- **现象**：极简模式下 Enter/双击/↑↓ 后详情在后台静默更新，界面零反馈，用户会怀疑"没生效"。
- **修复**：`_on_candidate_select` 末尾极简模式下 toast「详情已在后台更新 · Esc 返回完整模式查看」。顺带重构 `_toast`：旧实现用弹出时的文本快照恢复——会把 toast 期间的正常状态更新覆盖回旧值、连续 toast 相互闪断；改为单一 `_toast_after` 句柄防抖（连续 toast 只留最后一个到期回调），到期直接 `_refresh_status()` 重算真实状态。
- **验证**：探针实测极简下选中候选触发 toast、1.8s 后恢复常规文案；连续 toast 无闪烁。

### 12. 数据源切换/刷新无忙碌指示 ✅ 已修复
- **现象**：切换上游/混合需秒级构建（已后台线程化、有 busy 门控与 latest-wins），但 UI 上没有任何进行中提示，用户不知道"点了为什么没反应"。
- **修复**：切换启动时 3 个 radio 整体禁用（禁用态前景 #66788c 置灰，选中项保持绿底深字标识当前模式）+ 状态栏「正在切换/刷新数据源至…」文字（已有）；收尾（成功/失败）统一恢复可用。完成 toast 带耗时「已切换至「网络」 · 1.2s」（`time.monotonic` 计时）。
- **验证**：探针实测 `_set_radios_enabled` 禁用/恢复状态正确；全量回归通过。

---

## P2 —— 打磨项

### 13. 候选列表无 hover 行高亮 ✅ 已修复
`Candidate.Treeview` 增加 hover tag（CARD_HOVER 底色）跟随指针移动，选中行样式优先级更高不受影响。截图（极简态第 4 行）可见实际效果。

### 14. 错误提示用模态 messagebox ✅ 已修复（按建议分级）
数据源切换失败改为非阻断 toast：✗ 前缀 + 红字 + 停留 3.8s（radio 已自动回滚可直接重试）；导入/导出失败为用户主动的文件操作，按「低频错误保留模态」原则维持 messagebox。`_toast` 增加 error 参数（红前景、到期恢复）。

### 15. 空状态缺"可点的第一步" ✅ 已修复
`_render_detail_empty` 增加示例料号 chip（取库内前 3 条非空料号），点击填入查询框并自动触发防抖搜索，新用户零学习成本。

### 16. 无字号/缩放选项 ✅ 已修复（重启生效版）
「更多 ⋯ → 界面缩放」提供 85%/100%/115%/130% 单选，写入 `ui_zoom` 配置（乘在 DPI 因子上，0.8~1.6 越界回退）；布局常量启动时已实例化，选择后 toast 提示重启生效。热更版（整体重建布局）列为后续可选。

### 17. 详情卡双语标签冗余 ✅ 已修复
字段标签改纯中文主显（宽度 22→10 字符），英文全称进标签悬停 tooltip；字段值每行多得 ~80px 显示空间。

### 18. 主题与品牌完整性 ◐ 部分完成
已落地：标题栏显示版本号（v1.1.0，新增 APP_VERSION 常量）+「更多 ⋯ → 关于 ChipLookup」对话框（版本/模式/数据源概况）。浅色主题按原评审定位仍为长期项，未实施。

### 19. 查询输入框用 Consolas ✅ 已修复
Search.TEntry 字体改「Microsoft YaHei UI」；详情卡料号/型号展示区保持 Consolas 等宽对齐。

### 20. 窗口位置未记忆 ✅ 已修复
新增 `window_pos` 配置（"+x+y"），随尺寸防抖一并保存（只拖不缩放也会记录）；恢复时按屏幕边界收敛（x/y ≤ 屏幕尺寸-200px，保证标题栏露在屏内可拖回，防换屏丢窗口）。实现细节：保存端取 geometry() 的 WM 框架原点（与恢复端同基准，用客户区坐标会导致往返漂移，实测 +120+90 → +128+121）。

---

## 建议的实施顺序

1. **第一批（半天内可完成，收益最大）**：#2 DPI 感知、#3 蓝钮对比度、#8 状态文案去重、#11 极简反馈 toast —— **✅ 全部完成（2026-09-09）**
2. **第二批（1~2 天）**：#1 列宽权重策略、#4 焦点可见、#6 目标扩大、#7 tooltip、#12 忙碌指示 —— **✅ 全部完成（2026-09-09）**
3. **第三批（规划迭代）**：#5 minsize 与响应式收缩、#9 底栏重组、#10 分页重构、#13~#20 打磨项 —— **✅ 完成（2026-09-09；#18 浅色主题为长期项未实施，版本号/关于已落地）**

## 证据文件

- `screenshots/ui_review_default_full.png` —— 默认尺寸完整模式（列裁切/顶栏文案/底栏平铺）
- `screenshots/ui_review_minimal.png` —— 极简模式（全宽列可读，反证 #1 解法）
- `screenshots/ui_review_minsize.png` —— 最小尺寸（裁切最严重状态）
